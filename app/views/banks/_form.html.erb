<%= simple_form_for(@bank) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: "Nombre del Banco", required: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Guardar Banco" %>
  </div>
<% end %>
